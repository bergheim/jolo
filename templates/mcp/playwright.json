{
  "mcpServers": {
    "playwright": {
      "command": "pnpm",
      "args": [
        "dlx",
        "@playwright/mcp",
        "--executable-path",
        "/usr/bin/chromium-browser",
        "--no-sandbox",
        "--headless"
      ]
    }
  }
}
