#+TITLE: Shared Memory
#+DATE: 2026-02-15

Shared knowledge base for all agents working on this project.
Read on session start. Write discoveries, conventions, and patterns here.
Tag entries with keywords (e.g., =:auth:perf:musl:=) for searchability.

* Conventions

- Org-mode preferred over markdown for project docs                    :docs:
- Pre-commit hooks must pass before committing                         :workflow:
- System Python has no pip; use =uv= for everything                   :python:

* Patterns

- Secrets flow: =get_secrets()= -> =os.environ.update()= -> =${localEnv:...}= in devcontainer.json :jolo:
- =devcontainer_up(remove_existing=True)= tears down old container first — skip port checks :jolo:
- Motd runs in all tmux panes; per-window commands belong in =dev.yml= :tmux:
- Flavors use =-web= / =-bare= suffix convention: =typescript-web=, =rust-bare=, etc. :jolo:templates:
- =FLAVOR_LANGUAGE= maps flavor to base language for hooks/coverage :jolo:templates:
- =@elysiajs/static= serves files at =/public/= prefix by default but only works via real server, not =app.handle()= :elysia:bun:
- OpenSpec removed — SDD overhead not justified for this project; AGENTS.md + plan-before-act is sufficient :openspec:workflow:
- Do not force =TERM=tmux-direct= in devcontainer env/shell; let tmux negotiate TERM and enable =terminal-features ...:extkeys= for rich key input :tmux:term-keys:
- Host =~/.tmux.conf= =set -g terminal-features= (non-appending) clobbers =/etc/tmux.conf=; must use =set -as= :tmux:terminal:
- =clipboard= terminal-feature (tmux 3.2+) replaces the old =Ms= terminal-override for OSC 52 :tmux:clipboard:
- Mosh 1.4.0 supports OSC 52 but only with =c;= selection — tmux's =clipboard= feature emits =;;= (empty) which mosh drops :mosh:clipboard:
- For mosh clipboard: remove =clipboard= from =terminal-features=, use =Ms= override with hardcoded =c;=: =set -as terminal-overrides ',*:Ms=\\E]52;c%p1%.0s;%p2%s\\7'= :mosh:clipboard:tmux:
- Host-side/manual ops convention: label machines as =tux= (laptop/client) and =berghome= (home/server); provide grouped manual steps, verify, and rollback instead of running host mutations from container :host-ops:workflow:
- OSC 52 from Python: write to =/dev/tty= not =sys.stdout= — stdout may be piped during =jolo up= :clipboard:osc52:
- Alpine busybox =base64= wraps at 76 chars — pipe through =tr -d '\n'= for OSC 52 :alpine:clipboard:
- =devcontainer_up(remove_existing=True)= now force-removes container by name before launching, handles stale names after =podman system prune= :jolo:podman:
- Wayland Chromium in rootless Podman: renders but crashes (seccomp blocks =sched_getscheduler=, GPU subprocess dies) and input is broken. Not viable for interactive use. Headless (Playwright) works fine. :wayland:chromium:container:
- =find_git_root()= climbs to ancestor repos — check =target/.git= directly when the target must be a repo root :jolo:delete:
- =jolo tree= works standalone from any git repo — no =jolo up= on main needed first :jolo:worktree:
- Browser automation default is =playwright-cli= (stateful, artifact-first); =browser-check= remains for stateless checks :playwright:automation:
- For regressions: force =GOOD_SHA/BAD_SHA= diff + identical probes before root-cause claims; avoid guessing from logs alone :workflow:regression:
- agent-shell Codex integration requires =@zed-industries/codex-acp= (binary =codex-acp=); =@zed-industries/claude-code-acp= does not satisfy this :agent-shell:acp:codex:
- =jolo create= auto-formats scaffolded code before initial commit (biome/gofmt/ruff/rustfmt per language) :jolo:templates:formatting:
- =ruff= needs =--isolated= when run on template dirs — =pyproject.toml= has =={{VAR}}== placeholders that break TOML parsing :python:templates:
- Biome reformats =={{VAR}}== in JSX as ={{ VAR }}= (adds spaces) — wrap in string expression ={"{{VAR}}"}= to preserve placeholder :typescript:templates:biome:
- rustfmt is the canonical Rust formatter (no real alternatives); install via =apk add rustfmt= on Alpine :rust:formatting:
- Bun defaults =SO_REUSEPORT=true= on Linux — second server silently binds but is unreachable; set =reusePort: false= in templates :bun:elysia:
- =codex-acp= renamed: =@zed-industries/claude-agent-acp= → =@agentclientprotocol/claude-agent-acp= (pure TS now, no Rust/cargo build needed) :acp:codex:
- =@fission-ai/openspec= removed from Containerfile — openspec skills deleted, SDD replaced by AGENTS.md workflow :openspec:container:
- =jolo up --sync= now syncs AGENTS.md/CLAUDE.md/GEMINI.md if unmodified (hash-tracked in =.template-hashes.json=) :jolo:sync:
- Each container forwards =WORKTREE_PORTS= (3) extra ports above base for agent-shell worktrees; =replace_port_args()= in =container.py= :jolo:ports:
- =browser-check= defaults screenshots/PDFs to =scratch/= dir, creates it automatically :browser:automation:
- =open-terminal= installed via =uv tool install open-terminal= in Containerfile; starts on =PORT+3= in entrypoint :open-terminal:container:
- Open WebUI + Speaches provides self-hosted voice (STT: faster-whisper, TTS: Kokoro) on berghome :voice:openwebui:
- Open Terminal is the bridge: Open WebUI → tool server → devcontainer shell/filesystem :open-terminal:openwebui:
- =*.dev.glvortex.net= wildcard DNS → Caddy on berghome with basicauth + port regex (4000-5000 range) :caddy:networking:
- passt/pasta port forwarding: services reachable on tailscale IP but NOT on localhost from host :podman:networking:gotcha:
- Anthropic blocked third-party tools from using Claude Max subscription quota on 2026-04-04 :claude:api:billing:
- Claude Code ACP in Zed still uses subscription; only programmatic API calls require API key :claude:acp:zed:
- =jolo attach= sorts by MRU via =.devcontainer/.last-attach= mtime; touched on every =devcontainer_exec_tmux()= call :jolo:attach:
- =--sync= and =--new= merged into single =--recreate= flag; aligns with docker compose =--force-recreate= convention :jolo:cli:
- =jolo a= is alias for =jolo attach=; =jolo a --recreate= syncs config + rebuilds from anywhere via fzf :jolo:cli:
- Shared helpers: =_fzf_pick()=, =_setup_container_env()=, =_sync_config()=, =_last_attach_mtime()= eliminate duplication across commands :jolo:refactor:
- =load_config()= accepts =project_dir= param for non-cwd contexts (e.g., =jolo attach --recreate=) :jolo:config:
- Cross-agent reviews: unset =ANTHROPIC_API_KEY= and =OPENAI_API_KEY= when shelling out to =claude -p= or =codex= so CLI auth is used :workflow:agents:
- =.claude.json= injection: =hasTrustDialogAccepted= and =effortCalloutV2Dismissed= set during =setup_credential_cache()= :jolo:claude:
- Codex =config.toml= injection: =trust_level = "trusted"= set for container workspace path during setup :jolo:codex:
- Custom skills use =j:= namespace prefix (shortened from =jolo:=) :skills:workflow:
- ntfy notifications support up to 3 action buttons (semicolon-separated); current setup: "Open app" (web projects) + "View PR" or "View commit" :ntfy:notifications:
- =NOTIFY_APP=1= in =containerEnv= gates the "Open app" button; only set for =-web= flavors via =has_web= param :ntfy:jolo:
- =NOTIFY_PATH= env var or =notify --path /foo= appends to dev URL for page-specific notification links :ntfy:notifications:
- =_save_template_hashes= creates =.devcontainer/= early; =scaffold_devcontainer= must check for =devcontainer.json= existence, not directory :jolo:gotcha:
- =jolo a= shows stopped containers with state marker (=[exited]=); auto-starts before attaching :jolo:attach:
- Host =~/.zshrc= =HISTFILE=~/.histfile= overrides container env var; =.zshrc.container= must re-export =HISTFILE=$HOME/.zsh-state/.histfile= :zsh:histfile:gotcha:
- =notify set-path /foo= persists to =~/.notify-path=; notify reads it at send time — env vars don't inherit into hook subprocesses :ntfy:notifications:
- =~/.claude/plugins/= mounted RO from host into containers; stops LSP plugin prompts :claude:plugins:
- =OLLAMA_HOST= passed via =${localEnv:OLLAMA_HOST}= — must be set in host =~/.zshrc= (not =.zshenv=, which isn't mounted) :ollama:container:
- PostgreSQL server in image: =postgresql=, =postgresql-contrib=, =postgresql-pgvector= :postgresql:container:
- =db= script: init on first run (=.devcontainer/.pgdata/=), =postgres= superuser created, socket in =/tmp= :postgresql:db:
- =PGHOST=/tmp= set in =containerEnv= so =psql= connects without =-h= flag :postgresql:container:
- PostgreSQL is on-demand (=just db= / =db start=), NOT auto-started in entrypoint :postgresql:design:
- Elixir/Phoenix: =mix phx.new= needs =yes= piped (dir not empty prompt); =--force= removed in Phoenix 1.8 :elixir:phoenix:
- Phoenix 1.8 =config/dev.exs=: sed-patch username/hostname/ip after scaffold; =System.get_env("USER")= returns nil in config context :elixir:phoenix:gotcha:
- Justfile common recipes (browse, db) extracted to =templates/lang/common/justfile.common=, appended by =get_justfile_content()= :jolo:templates:
- =to_snake_case()= and =to_pascal_case()= in =templates.py= shared by =commands.py= and =templates.py= :jolo:naming:
- Containerfile: script COPYs (e, wt, motd, notify, db) placed after heavy layers (pnpm, cargo) to avoid cache busting; use =--chmod=755= for non-root COPY :containerfile:optimization:
- =agent-browser= daemon IPC hangs on Alpine/musl; upstream issue #1035, postponed to May 2026 :agent-browser:musl:
- Skills renamed from =jolo:= prefix to =j:= for brevity :skills:workflow:
- Screenshot workflow: =jolo-shot= on local machine → scp to =berghome:stash/= → agents read latest =shot-*.png= :workflow:screenshots:
- Always use =emacsclient --eval= for org state changes (TODO→DONE/CANCELLED) — never edit keywords manually. Use =org-todo=, =org-add-log-note=, =org-store-log-note= to get CLOSED timestamps and LOGBOOK entries with reasons :org:workflow:emacs:
- =org-element-map= lambda context breaks =org-todo= logging hooks — use =re-search-forward= + =beginning-of-line= to position point instead :org:emacs:gotcha:
- Flavor naming: =LANG= for bare, =LANG-web= for web (dropped =-bare= suffix); e.g. =typescript=, =typescript-web= :jolo:templates:
- =PUPPETEER_EXECUTABLE_PATH=/usr/bin/chromium= in =containerEnv= — pa11y, puppeteer, and anything using Puppeteer finds system Chromium :chromium:container:
- =pa11y= for automated WCAG 2.2 AA auditing; =just a11y= recipe in web templates :a11y:workflow:
- Image format preference: AVIF > WebP > PNG/JPEG; tools: =vips=, =avifenc=, =cwebp= :images:formats:
- tmux multi-client: removed =-d= from =tmux attach-session=, set =window-size largest= so biggest client sets dimensions :tmux:multi-client:
- =sync_skill_templates()= now removes skills not in template — cleans openspec leftovers on =--recreate= :jolo:sync:
- =db= script: auto-removes stale =postmaster.pid= if process is dead; handles comma-separated =unix_socket_directories= :postgresql:db:
- Claude plugins mounted RW (not RO) — installs persist to host, shared across all containers :claude:plugins:
- =axe-core/cli= uses Selenium+ChromeDriver (broken on Alpine); =pa11y= uses Puppeteer (works with system Chromium) :a11y:alpine:gotcha:
- =UV_LINK_MODE=copy= suppresses uv hardlink warning on Podman overlay filesystems :python:uv:podman:

* Terminal Image Display                                       :terminal:images:

- Three protocols: Sixel (1983 DEC), Kitty graphics (2017), iTerm2 inline images
- Kitty protocol is technically superior: 32-bit RGBA, caching, transparency, proper spec
- Sixel: palette-based color, no transparency, no caching, underspecified behavior
- Kitty/Ghostty refuse to implement sixel — push ecosystem toward kitty protocol
- tmux supports sixel natively (buggy: flickering, input-buffer-size limits) but NOT kitty graphics
- Kitty graphics through tmux uses *unicode placeholders* (U+10EEEE) — only kitty and Ghostty support this
- Container uses =kitty-kitten= package (25MB) for =kitten icat= command
- =icat= alias in container points to =kitten icat= (works through tmux with Ghostty/kitty host)
- =chafa= still installed as fallback (auto-detects protocol, falls back to Unicode block art)
- Host terminal must be Ghostty or kitty for images-in-tmux to work via kitty protocol
- WezTerm: supports all 3 protocols but maintenance stalled (last stable release Feb 2024)
- Alacritty: zero image protocol support, maintainers refuse to add it
- foot: Wayland-only (no X11), sixel only — not viable for dual Sway+i3 setup
- Ghostty: GTK4 (Wayland+X11), kitty graphics + unicode placeholders, Arch =extra= repo
- tmux =input-buffer-size= defaults to 1MB — large sixel images silently dropped; increase to 4MB+ if using sixel
- =TERM_PROGRAM= env var does not propagate over SSH — cannot detect host terminal via env vars in containers
- Mosh does not support Sixel or kitty graphics protocol; use SSH for terminal images, mosh for text/tmux resilience :mosh:images:
- mosh-server sets remote =TERM=xterm-256color=; SSH preserves local terminal TERM (e.g., =xterm-ghostty=) :mosh:term:

* Container Security Assessment (2026-02)                      :security:container:

Researched adding bubblewrap, Landlock, seccomp, nono etc. inside rootless Podman containers.
Conclusion: mostly ceremony for our setup. Details:

- Rootless Podman is already a strong boundary — recent runc escape CVEs (Nov 2025) are explicitly mitigated by rootless containers
- GPG keys: private keys never enter container, only agent socket (RO). Safe.
- SSH keys: not mounted at all. Safe.
- =.gitconfig=, =.zshrc=, =.config/gh=, =.config/tmux=: all mounted RO
- Claude =.credentials.json=: mounted RW (token refresh needs persist to host)
- Gemini/Codex/Pi creds: copied RW into =.devcontainer/= cache dirs
- API keys (=ANTHROPIC_API_KEY=, =OPENAI_API_KEY=, =GH_TOKEN=): env vars, readable by any process
- The "confused deputy" problem: agent NEEDS credentials to function, same credentials can be abused via prompt injection
- Landlock/bwrap inside container = nesting namespace isolation for marginal gain, high breakage risk
- Network filtering breaks legitimate workflows; agent can exfiltrate through allowed channels (git push, API calls)
- Simon Willison's "Lethal Trifecta": private data + untrusted content + external comms = fundamentally vulnerable
- OpenAI admits prompt injection "unlikely to ever be fully solved"
- Real improvements: scoped git tokens, credential rotation, reviewing agent output — not more OS sandbox layers
- Egress monitoring (logging, not blocking) is the one addition that could provide visibility without breakage

* Gotchas

- Grep/Glob tools sometimes miss =_jolo/*.py= — use =**/_jolo/**/*.py= or =ls= :tooling:
- Alpine uses musl; glibc .so/.elc files are not compatible           :musl:
- =gh= auth token is in OS keyring, not =hosts.yml= — need =gh auth token= to extract :gh:
- =templates/.gitignore= has a blanket =public/= ignore (Gatsby) — fixed to =public/*.css= + =public/*.js= instead :jolo:gitignore:
- Codex =exec= ignores piped stdin diff — it runs =git diff= internally and only sees unstaged changes :codex:review:
- Gemini misreads =@= in npm package names as leading spaces in diffs :gemini:review:
- npm package =playwright-cli= is deprecated/no-bin; current package is =@playwright/cli= and needs explicit =executablePath= config to use system Chromium on Alpine :playwright:alpine:
- =emacsclient -nw= crash on pgtk was =bergheim/disable-pgtk-im= config bug, not a pgtk issue — container uses =emacs-pgtk-nativecomp= :emacs:pgtk:
- Tmux 3.6 sends CSI u but doesn't respond to KKP probe — =kkp.el= must be force-enabled; =tty-setup-hook= alone insufficient in daemon mode (elpaca async loading race) :emacs:tmux:keys:
- =term-keys= (CyberShadow) uses =\e[27;mod;code~= format, not CSI u — does nothing in tmux; kkp.el is the correct decoder for tmux =extkeys= :emacs:tmux:keys:
