#+TITLE: TODO
#+DESCRIPTION: Open tasks for the emacs-container project

* Tasks
** DONE Add regression triage skill + container smoke guard
   Added =templates/.agents/skills/regression-triage/SKILL.md= to enforce
   commit-range diagnosis (=GOOD_SHA= -> =BAD_SHA=) with reproducible probes.
   Added build-time smoke check in =Containerfile=:
   =ruby -e "require 'thor'"= and =tmuxinator version=.
** DONE Switch browser automation default to Playwright CLI
   Replaced MCP-first guidance with =playwright-cli= sessions (stateful, token-efficient artifacts in =.playwright-cli/=).
   Added scaffolded =.playwright/cli.config.json= for Alpine system Chromium.
   Dropped =templates/mcp/playwright.json= and replaced =@playwright/mcp= with =@playwright/cli= in =Containerfile=.
** TODO Wayland-aware =just browse= — open Chromium on host display
   Branch: =feat/wayland-browse= (2 commits, reviewed, not merged).
   When =WAYLAND_DISPLAY= is set, opens system Chromium with =--ozone-platform=wayland=.
   Update behavior: stop auto URL copy on =jolo up/create=. =just browse= should
   open Chromium on Wayland; clipboard copy should be explicit/opt-in.
   Falls back to clipboard-only or URL print without Wayland.
   Also toggles headless behavior appropriately for local browser tooling on Wayland.
** TODO Add Phoenix LiveView scaffold support (Elixir)
   Include dev-mode LiveView hot reload and browser live reload defaults.
   Update =Containerfile= to include Elixir/Erlang/Phoenix tooling needed by scaffolded projects.
** TODO Investigate network egress monitoring for devcontainers
   Log outbound connections from agent containers for visibility without blocking.
   Not urgent — current rootless Podman isolation is strong. This is about
   observability, not enforcement. Options to research:
   - Podman =--log-driver= with network event logging
   - Lightweight transparent proxy (mitmproxy, squid) logging connections
   - eBPF-based connection tracking (=bpftrace=, =cilium=)
   - Simple =ss= / =conntrack= polling from host
   Goal: after-the-fact audit trail, not real-time blocking.
** TODO Use scoped GitHub tokens per project
   Replace broad =GH_TOKEN= with fine-grained personal access tokens
   scoped to specific repos. Reduces blast radius if a token is exfiltrated.
   GitHub fine-grained tokens support per-repo permissions since 2023.
** TODO Add ntfy desktop notifications on Linux
   Install =ntfysh-bin= (AUR), configure =~/.config/ntfy/client.yml= to subscribe
   to the =jolo= topic and pipe to =notify-send=, enable =ntfy-client= user service.
   #+begin_src yaml
   subscribe:
     - topic: jolo
       command: 'notify-send -a ntfy "$t" "$m"'
   #+end_src
   #+begin_example
   yay -S ntfysh-bin
   systemctl --user enable --now ntfy-client
   #+end_example
** TODO Voice-driven development on the go (Android → devcontainers)
   Goal: develop with Claude from your phone using voice — speak prompts,
   hear results. See =docs/2026-04-07-voice-dev-on-the-go.org= for full research.

*** TODO Phase 1: Merge TTS branch + Remote Control workflow
    :PROPERTIES:
    :BRANCH: feat/research-tts
    :END:
    1. Merge =feat/research-tts= (1 commit ahead) — TTS output via Kokoro + ntfy
    2. Verify Kokoro at =berghome.ts.glvortex.net:4014= — check if it's Kokoro-FastAPI or bare model
    3. Test: =claude --remote-control= in container → connect from Claude Android app
    4. Use Android keyboard voice typing for input (works today, zero code)
    5. Verify ntfy audio attachments play on phone

*** TODO Phase 2: PWA voice interface
    Build a lightweight PWA with a big mic button:
    - Web Speech API (Chrome Android) for continuous STT
    - WebSocket relay to feed text into Claude Code session
    - Kokoro TTS for streaming audio responses back
    - Host at =p<PORT>.dev.glvortex.net= (Caddy + basicauth already set up)
    - Consider upgrading Kokoro to [[https://github.com/remsky/Kokoro-FastAPI][Kokoro-FastAPI]] for OpenAI-compatible API

*** TODO Phase 3: Self-hosted STT (optional, privacy)
    Replace Chrome's Google-backed STT with self-hosted faster-whisper on berghome.
    PWA records audio → POST to whisper API → text → session.
    All voice processing on own infrastructure.
** DONE Install codex ACP for agent-shell
   :PROPERTIES:
   :DATE: 2026-03-01
   :COMMIT: df766d9
   :END:
   Added =@zed-industries/codex-acp= to =Containerfile= global pnpm tools so
   =codex-acp= is present for =agent-shell=. Rebuild required for runtime use.
** DONE Fix =container/e= to use emacsclient + daemon
   :PROPERTIES:
   :DATE: 2026-03-24
   :END:
   Changed =e= to use =emacsclient --alternate-editor=""= which auto-starts daemon.
   Enables =emacsclient -e= for eval from agent shells (agent-shell, claude-code).
   Also fixed =xterm-mouse-mode= in style.el (was =setq= instead of function call).
** DONE Fix emacsclient -nw crash (was config bug, not pgtk)
   :PROPERTIES:
   :DATE: 2026-03-26
   :END:
   - Root cause: =bergheim/disable-pgtk-im= in =completion.el= needed =(display-graphic-p)= guard
   - Container stays on =emacs-pgtk-nativecomp= (gtk3 switch was unnecessary)
   - Added GNU =grep= package (BusyBox grep lacks =--exclude-dir= for zshrc alias)
   - Discovered OSC 52 =interprogram-cut-function= for Emacs → host clipboard (not yet in config)
** DONE Fix =kkp.el= tmux probe failure — force-enable CSI u decoders
   =tty-setup-hook= + =advice-add :after kkp--terminal-setup= both in host
   =base.el= but still not working on fresh daemon start. Manual
   =(kkp-force-enable-on-tty)= call works instantly. Likely cause: elpaca
   loads kkp async, so =tty-setup-hook= fires before the function exists,
   and the advice on =kkp--terminal-setup= either doesn't fire or sees
   wrong terminal context. Next: add debug logging to trace hook/advice
   execution on daemon restart.
   =term-keys= package is also loaded but irrelevant (different escape format).
   See RESEARCH.org "Shift+Space sends literal u" for full analysis.
** DONE Add OSC 52 clipboard to Emacs config
   Add =interprogram-cut-function= with OSC 52 to =modules/base.el= inside
   =(use-package emacs)= block, guarded by =(getenv "EMACS_CONTAINER")=.
   See RESEARCH.org for the elisp snippet.
** TODO Symlink .claude/ and .agents/ into git worktrees
   Agent-shell worktrees (=agent-shell-new-worktree-shell=) and jolo worktrees
   don't get =.claude/= or =.agents/= since they're gitignored. Without them,
   custom slash commands (=/afk=, etc.) silently stall. Need Emacs advice on
   =agent-shell-new-worktree-shell= to symlink from the main repo root and
   set per-worktree =PORT= (base + offset). Port range forwarding is done
   (=WORKTREE_PORTS=3= extra ports per container). Elisp advice is designed
   but not yet written into host config.
** TODO Add agent-shell keybinding =SPC oa= for send-dwim
   =agent-shell-send-dwim= sends region or error at point — closest to
   =gptel-add= (=SPC ja=). Also consider =SPC oC= for =agent-shell-cycle-session-mode=.
   Some existing bindings reference renamed functions (now aliases):
   =agent-shell-search-history= → =shell-maker-search-history=,
   =agent-shell-rename-buffer= → =shell-maker-rename-buffer=,
   =agent-shell-insert-file= / =agent-shell-send-current-file= → =agent-shell-send-file=.
** DONE Delete openspec skills and =openspec/= directory
   Removed 11 =openspec-*= skills, 10 =opsx:*= command aliases, and =openspec/= directory.
** TODO Automate wildcard cert renewal for =*.dev.glvortex.net=
   :PROPERTIES:
   :DEADLINE: 2026-07-01
   :END:
   Current cert expires 2026-07-06 (issued manually via certbot DNS-01).
   Set up =acme-dns= on berghome to automate renewal:
   1. Run acme-dns (Docker)
   2. Add permanent CNAME in uniweb: =_acme-challenge.dev= → acme-dns
   3. Configure certbot to use acme-dns hook for unattended =certbot renew=
   Also watch DNS-PERSIST-01 (Let's Encrypt Q2 2026) as a simpler alternative.
   Caddy config: =*.dev.glvortex.net= with basicauth, port range 4000-5000,
   reverse_proxy to =100.64.0.2:{port}=.
** TODO Verify NOTIFY_PATH env inheritance in automated hooks
   =export NOTIFY_PATH=/foo= in agent shell may or may not be inherited by
   Claude Code's hook subprocess. If not, update hook commands to pass
   =--path "$NOTIFY_PATH"= explicitly. Threshold was temporarily set to 1s
   for testing — reset to 20s after verification.
** TODO Add ollama integration (OLLAMA_HOST=http://berghome:11434)
   Ollama runs on berghome with gemma4:26b + others. Pass =OLLAMA_HOST= env
   to containers. Research shows ollama overhead is ~5-13% vs raw llama.cpp,
   acceptable for multi-model switching. Try =OLLAMA_LLAMA_CPP=1= if a
   model is slow. See =RESEARCH.org= "Ollama vs llama.cpp" for benchmarks.
** TODO Get agent-browser working with container Chromium
   [[https://github.com/vercel-labs/agent-browser][agent-browser]] should support custom browser executables via config.
   Latest commits suggest it works, but it doesn't in practice.
   See: https://github.com/vercel-labs/agent-browser?tab=readme-ov-file#custom-browser-executable
** DONE Diagnose Elpaca bootstrap regression after image update
   :PROPERTIES:
   :DATE: 2026-03-01
   :END:
   Root cause: ~elpaca-use-package-by-default~ no longer applied; packages were
   not ensured by default. Fix is ~use-package-always-ensure t~ in the
   ~elpaca-use-package~ bootstrap block.
